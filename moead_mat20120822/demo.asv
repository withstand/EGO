function demo()

mop = testmop('KNO1',30);

pareto = moead( mop, 'popsize', 20, 'niche', 4, 'iteration', 200, 'method', 'te');

hold
pareto = moead( mop, 'popsize', 100, 'niche',20, 'iteration', 200, 'method', 'te');

%pareto = moead( mop, 'popsize', 100, 'niche', 20, 'iteration', 200, 'method', 'ws');

end